package Tests;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;
import Tran.*;
public class MyTests {
    //check that numbers followed by no whitespaces are properlu handled (i.e. in tran 1.3a != 1.3*a due to syntax)
}
